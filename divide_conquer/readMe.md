# Inversion Count #

Given an unsorted array $A$  of size $n$ with values $1$ to $n$. We will write
a program that uses the `divide and conquer` paradigm to count the number of
inversions in a $\mathcal{nlog(n)}$


